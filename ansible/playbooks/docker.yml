---

- name: Install Docker Engine and configure base system
  hosts: monitoring

  gather_facts: true

  roles:
    - role: common
      tags: [common, base]

    - role: docker
      tags: [docker]
