---

all:
  children:

    monitoring:
      hosts:
        monitoring-server:
          ansible_host: "{{ lookup('env', 'MONITORING_HOST') | default('REPLACE_WITH_EC2_PUBLIC_IP') }}"

          ansible_user:                ubuntu
          ansible_ssh_private_key_file: ~/.ssh/monitoring-key.pem
